import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    type: "light",
    primary: {
      main: "#4e7cc4",
    },
    secondary: {
      main: "#ffa500",
    },
    // background: {
    //   default: "#414040",
    // },
  },
});

export default theme;
